<?xml version="1.0" encoding="utf-8"?>
<!--
  Android's network-security-config has no CIDR/IP-range syntax, so a private-LAN
  allowlist (what the iOS build effectively gets from ATS's local-network exception)
  can't be expressed as a domain list. This is a local-first dev tool not distributed
  via Play, so cleartext is allowed app-wide; the explicit entries below just document
  the hosts this app actually talks to.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="true">ts.net</domain>
    </domain-config>
</network-security-config>
